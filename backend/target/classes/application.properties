# --- Database Connection (Neon) ---
# I updated the URL to use your Neon "Pooler" endpoint.
# Note: The database name is set to 'neondb' (default). If you created a DB named 'nutzycraft' in Neon, change 'neondb' to 'nutzycraft'.
spring.datasource.url=jdbc:postgresql://ep-snowy-hat-a1duupft-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Copy the password by clicking the "Eye" icon in the Neon dashboard
spring.datasource.username=neondb_owner
spring.datasource.password=npg_UE1cPi7fyukJ

# --- Hibernate / JPA Settings ---
# 'update' is fine for development, but be careful in production (it alters tables automatically)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# It is good practice to specify the dialect, though Spring often detects it automatically
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Server ---
server.port=8080